# Code generated by protoc-gen-protokit. DO NOT EDIT.
# versions:
# 	protoc-gen-protokit test
# 	protoc test
# source: (every service in this run)
#
# target: ros
# field slots below are recorded in buffers.lock; edit the .proto, not this file
#
# protokit — https://github.com/the-protobuf-project/protokit
# Topics derived from server-streaming methods.
#
# ROS declares the topic-to-type binding in launch files and in code, never in
# the IDL, so this manifest is the only schema-derived record of it. It is also
# what eCAL publisher and subscriber generation will read: an eCAL channel and a
# ROS topic are the same idea under two names.

topics:
  - name: "sensor_readings"
    type: "sensors_msgs/StreamReadingsResponse"
    source: "SensorService.StreamReadings"
    doc: "StreamReadings publishes every reading a sensor produces."
